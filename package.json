{
  "name": "inforario-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "dev:web": "npm run dev:web --prefix frontend",
    "start:supa": "npm run start:supa --prefix frontend"
  }
}
